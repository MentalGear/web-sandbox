{
  "name": "lofi-web-sandbox",
  "version": "1.0.0",
  "description": "Local-first web sandbox",
  "main": "server.ts",
  "scripts": {
    "start": "bun run server.ts",
    "test": "bun x playwright test --config=research/playwright.config.ts"
  },
  "devDependencies": {
    "playwright": "^1.58.1",
    "@playwright/test": "^1.58.1",
    "@types/bun": "^1.3.8"
  }
}
